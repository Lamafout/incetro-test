class OrganisationsMeta{
  int totalCount;
  int pageCount;
  int currentPage;
  int perPage;

  OrganisationsMeta({required this.totalCount, required this.pageCount, required this.currentPage, required this.perPage});
}